# nature card art

Add individual spell images here using the spell id as filename (for example seed-shot.png). Register each image in src/components/cards/spellArt.ts. Missing images intentionally render an empty art area.
